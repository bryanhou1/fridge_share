<div class="row">
  <div class="col-5">
    <h1>Items</h1>
    <ul id="items_li"></ul>
  </div>
  <div class="col-5" id="item_details_container">
  </div>
  <div class="col-2" id="messages_container">
  </div>
</div>


<% render partial: 'show', locals: {items: @items}%>
<br>

<% if user_signed_in? %>
	<%=link_to 'New Item for the user', new_user_item_path(current_user)%>
<% else %>
	<%= link_to 'New Item', new_item_path %>
<% end %> | <a href="#" id="new_item_btn">New Item on page</a>

