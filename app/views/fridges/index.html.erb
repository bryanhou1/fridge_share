

<div class="container">
  <div class="row">
    <h1>Fridges</h1>
  </div>
  <div class="row">
    <div class="col-1">
      <a href="#" id="new_fridge_btn" class="btn btn-secondary btn-sm">New fridge</a>
    </div>
  </div>
  <div class="row">
      <div id="messages_container"></div>
  </div>
  <div class="row">
    <div class="col-4">
      <ul id="fridges_li" class="list-unstyled d-flex flex-wrap align-content-center"></ul>
    </div>
    <div class="col-8">
      <div id="display_fridge"></div>
      <div id="display_fridge_form"></div>
    </div>
  </div>
</div>

<!-- <table border=1 bordercolor=black>
  <thead>
    <tr>
      <th>Fridge ID</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody id="fridges_tbody">

    <% @fridges.each do |fridge| %>
      <tr>
        <td><%= fridge.id %></td>
        <td><%= fridge.name %></td>
        <td><%= link_to 'Show', fridge %></td>
        <td><%= link_to 'Edit', edit_fridge_path(fridge) %></td>
        
        <td><%= link_to 'Destroy', fridge, method: :delete, data: { confirm: 'Are you sure?' } if fridge.items == [] %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

