<h1><%= "Fridge #{params[:id]} - #{@fridge.name}" %> </h1>

<h2>Items</h2>

<%= render partial: 'items/show', locals: {items: @fridge.items}%>

<%= link_to 'Edit', edit_fridge_path(@fridge) %> |
<%= link_to 'Back', fridges_path %>