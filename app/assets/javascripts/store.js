const store = {
	state: {
		fridges: [],
		items: [],
		users: [],
	},
	getState: function() {
		return this.state
	},
	// dispatch: function(actionCreator) {
	// 	var action = actionCreator()
	// 	this.reducer(action)
	// },
	// reducer: function(ob) {

	// }
}
